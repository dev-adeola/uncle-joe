export const metadata = {
  title: "Create new offer",
  description: "Create new offer for other ratefy members to trade with you",
};

export default function FormAndChatLayout({ children }) {
  return (
    <div className="w-full h-screen">
      {children}
    </div>
  );
}
